[package]
name = "mixarchy-ctl"
version = "1.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lofty = "0.21"
walkdir = "2.5"
rand = "0.8"
image = { version = "0.25", features = ["jpeg", "png"] }
